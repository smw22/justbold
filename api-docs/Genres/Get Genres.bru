meta {
  name: Get Genres
  type: http
  seq: 1
}

get {
  url: {{domain}}/genres
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
