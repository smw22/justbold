meta {
  name: Get JWT
  type: http
  seq: 2
}

post {
  url: {{domain}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user@email.com",
    "password": "admin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
