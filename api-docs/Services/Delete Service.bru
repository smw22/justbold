meta {
  name: Delete Service
  type: http
  seq: 5
}

delete {
  url: {{domain}}/services/:servicesId
  body: none
  auth: bearer
}

params:path {
  servicesId: {{service_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
