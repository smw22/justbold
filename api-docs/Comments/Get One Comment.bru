meta {
  name: Get One Comment
  type: http
  seq: 2
}

get {
  url: {{domain}}/comments/:commentId
  body: none
  auth: bearer
}

params:path {
  commentId: {{comment_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
