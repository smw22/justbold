meta {
  name: Get One User
  type: http
  seq: 2
}

get {
  url: {{domain}}/user/:userId
  body: none
  auth: bearer
}

params:path {
  userId: {{user_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
