meta {
  name: Delete Comment
  type: http
  seq: 5
}

delete {
  url: {{domain}}/comments/:commentId
  body: none
  auth: bearer
}

params:path {
  commentId: {{comment_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
