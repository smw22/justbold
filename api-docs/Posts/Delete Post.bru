meta {
  name: Delete Post
  type: http
  seq: 5
}

delete {
  url: {{domain}}/posts/:postId
  body: none
  auth: inherit
}

params:path {
  postId: {{post_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
