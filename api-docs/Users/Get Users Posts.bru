meta {
  name: Get Users Posts
  type: http
  seq: 5
}

get {
  url: {{domain}}/user/:userId/posts
  body: none
  auth: bearer
}

params:path {
  userId: {{user_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
