meta {
  name: Get Users Reviews
  type: http
  seq: 6
}

get {
  url: {{domain}}/user/:userId/reviews
  body: none
  auth: bearer
}

params:path {
  userId: {{user_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
