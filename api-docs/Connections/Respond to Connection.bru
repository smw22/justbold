meta {
  name: Respond to Connection
  type: http
  seq: 3
}

patch {
  url: {{domain}}/connections/:connectionId
  body: json
  auth: bearer
}

params:path {
  connectionId: 
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "accept" // Accent or decline
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
