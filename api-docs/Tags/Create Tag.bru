meta {
  name: Create Tag
  type: http
  seq: 3
}

post {
  url: {{domain}}/tags
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "title": "Gamer 1"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
