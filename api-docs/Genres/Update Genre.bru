meta {
  name: Update Genre
  type: http
  seq: 4
}

patch {
  url: {{domain}}/genres/:genreTitle
  body: json
  auth: bearer
}

params:path {
  genreTitle: 
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "title": "Updated Tag title"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
