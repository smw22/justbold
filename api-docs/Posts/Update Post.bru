meta {
  name: Update Post
  type: http
  seq: 4
}

patch {
  url: {{domain}}/api/posts/:postId
  body: json
  auth: inherit
}

params:path {
  postId: {{post_id}}
}

body:json {
  {
    "title": "Another Title",
    "content": "updated content"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
