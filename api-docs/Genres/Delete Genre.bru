meta {
  name: Delete Genre
  type: http
  seq: 5
}

delete {
  url: {{domain}}/genres/:genreTitle
  body: none
  auth: bearer
}

params:path {
  genreTitle: 
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
