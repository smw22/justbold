meta {
  name: Delete Collaboration
  type: http
  seq: 5
}

delete {
  url: {{domain}}/collaborations/:collaborationId
  body: none
  auth: bearer
}

params:path {
  collaborationId: {{collaboration_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
