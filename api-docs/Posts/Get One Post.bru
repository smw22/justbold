meta {
  name: Get One Post
  type: http
  seq: 2
}

get {
  url: {{domain}}/posts/:postId
  body: none
  auth: bearer
}

params:path {
  postId: {{post_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
