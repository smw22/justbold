meta {
  name: Update Tag
  type: http
  seq: 4
}

patch {
  url: {{domain}}/api/tags/:tagTitle
  body: json
  auth: inherit
}

params:path {
  tagTitle: {{tag_title}}
}

body:json {
  {
    "title": "Updated Tag title"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
