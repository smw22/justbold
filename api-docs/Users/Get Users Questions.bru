meta {
  name: Get Users Questions
  type: http
  seq: 7
}

get {
  url: {{domain}}/user/:userId/questions
  body: none
  auth: bearer
}

params:path {
  userId: {{user_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
