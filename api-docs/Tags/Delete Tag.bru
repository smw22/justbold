meta {
  name: Delete Tag
  type: http
  seq: 5
}

delete {
  url: {{domain}}/tags/:tagTitle
  body: none
  auth: bearer
}

params:path {
  tagTitle: {{tag_title}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
