meta {
  name: Remove a Like
  type: http
  seq: 7
}

delete {
  url: {{domain}}/posts/:postId/likes
  body: none
  auth: bearer
}

params:path {
  postId: {{post_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
