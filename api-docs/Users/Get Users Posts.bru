meta {
  name: Get Users Posts
  type: http
  seq: 2
}

get {
  url: {{domain}}/api/users/:userId/posts
  body: none
  auth: bearer
}

params:path {
  userId: {{user_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
