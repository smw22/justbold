meta {
  name: Like a post
  type: http
  seq: 8
}

get {
  url: {{domain}}/posts/:postId/likes
  body: none
  auth: bearer
}

params:path {
  postId: {{post_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
