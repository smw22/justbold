meta {
  name: Like a Post
  type: http
  seq: 7
}

post {
  url: {{domain}}/comments/:commentId/likes
  body: none
  auth: bearer
}

params:path {
  commentId: {{comment_id}}
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
